/**
 * /heo/notebook/letter/write — Letter writing page (CP9)
 * Heo writes her weekly letter or plays two truths.
 */
import { LetterWriter } from "@/components/notebook/LetterWriter";

export const dynamic = "force-dynamic";

export default function LetterWritePage() {
  return <LetterWriter />;
}
